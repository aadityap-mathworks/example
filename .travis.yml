language: c
script:
  - clang -coverage -O0 hello.c -o hello
  - ./hello
  - gcno --version
  - gcc --version
  - gcda --version
  - gcov hello.c

after_success:
  - bash <(curl -s https://codecov.io/bash)
